<resources>

    <!-- Base application theme. -->
    <style name="AppMap" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/ColorBAR</item>
        <item name="colorPrimaryDark">@color/ColorBAR</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorNAvigation</item>
        <item name="colorPrimaryDark">@color/colorNAvigation</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

</resources>
